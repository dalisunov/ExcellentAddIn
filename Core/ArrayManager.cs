namespace ExcellentAddIn.Core
{
    public class ArrayManager
    {
        public T[] CreateArray<T>(int size)
        {
            return new T[size];
        }

        // Пример: заполнение массива или преобразование
    }
}
